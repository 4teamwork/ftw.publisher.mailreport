Test the utils:

    >>> from ftw.publisher.mailreport.utils import *
    >>> from datetime import datetime
    >>> is_interval_expired()
    True
    >>> get_last_notification_date() == None
    True
    >>> set_last_notification_date_to_now()
    >>> isinstance(get_last_notification_date(), datetime)
    True
    >>> is_interval_expired()
    False
